[defaults]
inventory = inventory.ini
roles_path = roles
host_key_checking = False
retry_files_enabled = False
gathering = smart
result_format = yaml
interpreter_python = auto_silent
# The doctor is read-only; no facts are written to targets.

[ssh_connection]
pipelining = True
